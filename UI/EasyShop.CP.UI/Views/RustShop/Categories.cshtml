@model EasyShop.Domain.ViewModels.Shop.Rust.RustShopViewModel
@{
    ViewData["Title"] = "Edit rust Shop";
    Layout = "Partial/_RustEditLayout";
}

<div class="well text-center">
    <div class="row">
        <div class="col-md-8 col-md-offset-2 text-center">
            <h2>Main settings</h2>
        </div>
    </div>
    <div class="row">
        <div class="col-lg-12">
            <div class="panel-body">
                <div class="table-responsive">
                    <table class="table table-responsive table-center">
                        <thead>
                            <tr>
                                <th>Name</th>
                                <th>Items in category</th>
                                <th>Edit</th>
                                <th>Delete</th>
                            </tr>
                        </thead>
                        <tbody>
                            @{
                                foreach (var categoryModel in Model.RustShopCategories.Categories)
                                {
                                    <tr>
                                        <td>
                                            <strong>@categoryModel.Name</strong>
                                        </td>
                                        <td>
                                            <strong>@categoryModel.AssignedItemsCount</strong>
                                        </td>
                                        <td>
                                            <a asp-area="" asp-controller="RustShop" asp-action="EditCategory" asp-route-shopId="@Model.Id" asp-route-categoryId="@categoryModel.Id"><span class="icon icon-pencil icon-lg icon-fw"></span></a>
                                        </td>
                                        <td>
                                            <a id="deleteButton_@categoryModel.Id" href="#" onclick="deleteShopConfirmationDialog('@categoryModel.Id', true)" style="color: red"><span class="icon icon-times icon-lg icon-fw"></span></a>
                                            <span id="confirmDeleteSpan_@categoryModel.Id" style="display: none">
                                                <span>Are you sure you want to delete?</span>
                                                <span>
                                                    <a asp-area="" asp-controller="RustShop" asp-action="DeleteCategory" asp-route-shopId="@categoryModel.Id" style="color: green"><span class="icon icon-check icon-lg icon-fw"></span></a>
                                                </span>
                                                <span>
                                                    <a style="color: red; cursor: pointer" onclick="deleteConfirmationDialog('@categoryModel.Id', false)"><span class="icon icon-times icon-lg icon-fw"></span></a>
                                                </span>
                                            </span>
                                        </td>
                                    </tr>
                                }
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts
{
    <script src="~/js/customScripts/DeleteConfirmationDialog.js" asp-append-version="true"></script>
}
